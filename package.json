{
  "name": "@ganesh47/totp",
  "version": "0.0.0",
  "description": "TOTP (Time-based One-time Password Algorithm) implementation in TypeScript for use with cloudflare-workers",
  "type": "module",
  "devDependencies": {
    "typescript": "^5.0.2",
    "vitest": "^0.29.3"
  },
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
